<main class="flex-row">
  <header class="flex-1 flex pl-2 pr-2 items-center justify-around bg-[#0891b2] p-5">
    <div class="">
      <h1 class="font-bold text-5xl text-center text-white pb-5"><%= @owner.name %></h1>
      <img src=<%= @owner.gravatar_url %> alt="image_of_owner" class="h-64">
    </div>
    <div class=" flex-row font-medium bg-white p-5">
      <h2 class="flex-1 text-3xl pb-5">About me</h2>
      <p class="flex-1 pb-5"><%= @owner.bio %></p>
      <div class="flex-1 pb-5 flex justify-around w-20">
        <%= render 'github_logo' %>
        <%= render 'linkedin_logo' %>
      </div>
    </div>
  </header>
  
  <main class="flex-1 border-green-600 p-2">
    <h2 class="text-3xl font-medium text-center md:text-left pb-5">My work</h2>
    <div class="pb-5 grid gap-10 grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
      <% @owner.projects.each do |project| %>
        <%= render 'card', project: project %>
      <% end %>
    </div>
  </main>
  
  <footer class="flex-1 bg-[#0891b2] text-white pl-5 pt-10 pb-10">
    <h2 class="text-3xl">Contact me</h2>
    <p class="pt-5 pb-5">Please get in touch if you think our work could be mutually beneficial!</p>
    <p class="pb-5">〒140-0002 東京都品川区東品川</p>
    <p class="pb-5"><%= @owner.email %></p>
      <div class="flex justify-around w-20">
        <%= render 'github_logo' %>
        <%= render 'linkedin_logo' %>
      </div>
  </footer>
</main>
